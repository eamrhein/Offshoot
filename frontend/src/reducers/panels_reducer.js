

const initialState = {

}

const panelsReducer = (state = initialState, action) => {
    switch (action.type) {

    
        default:
            return state
    }
}

export default panelsReducer;